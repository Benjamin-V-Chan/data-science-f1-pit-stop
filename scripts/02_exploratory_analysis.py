# - import pandas, matplotlib.pyplot, seaborn, os
# - define load_processed(path)
# - define summary_stats(df): compute describe(), save to CSV
# - define plot_distributions(df, cols): for each col, plot histogram, save figure
# - define plot_correlations(df, cols): compute corr matrix, plot heatmap, save figure
# - in main:
#     * load processed data
#     * run summary_stats
#     * plot distributions for key numeric columns
#     * plot correlation heatmap
